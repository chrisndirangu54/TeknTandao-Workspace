name: donor_firebase
description: Shared Firebase-backed operational panels for retained donor applications.
publish_to: none
version: 0.1.0
environment:
  sdk: '>=3.3.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.15.0
  firebase_auth: ^5.6.0
  cloud_firestore: ^5.6.0
  cloud_functions: ^5.6.0
